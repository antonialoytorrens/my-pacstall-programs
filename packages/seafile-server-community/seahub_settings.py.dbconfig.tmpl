# /opt/seafile/conf/seahub_settings.py - generated by dbconfig-common (ucf-tracked).
# `dpkg-reconfigure seafile-server-community` for DB changes; SECRET_KEY lives in .env.

import os

DEBUG = False

# SECRET_KEY written once to .env by postinst (shared stack-wide);
# read from env here rather than duplicated into this ucf-managed file.
SECRET_KEY = os.environ['SECRET_KEY']

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': '_DBC_DBNAME_',
        'USER': '_DBC_DBUSER_',
        'PASSWORD': '_DBC_DBPASS_',
        'HOST': '_DBC_DBSERVER_',
        'PORT': '_DBC_DBPORT_',
        'OPTIONS': {'charset': 'utf8mb4'},
    }
}

CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/0',
        'OPTIONS': {'CLIENT_CLASS': 'django_redis.client.DefaultClient'},
    }
}

COMPRESS_CACHE_BACKEND = 'default'

FILE_SERVER_ROOT = 'http://localhost:8082'
SERVICE_URL = 'http://localhost'

MEDIA_ROOT = '/opt/seafile/seahub-data/media'
