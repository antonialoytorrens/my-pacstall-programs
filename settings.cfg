# Package build settings for my-pacstall-programs
# Packages come from packages/*/ ; distros from docker/Dockerfile* (see scripts/discover.sh).

# Docker Compose
COMPOSE := docker compose
SERVICE := pacstall-deb-builder
